apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backend-models-pvc
  labels:
    app: semantic-backend
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
